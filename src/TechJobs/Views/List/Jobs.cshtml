<h1>@ViewBag.title</h1>

<h3>@ViewBag.Jobs.Count Result(s)</h3>


@foreach (Dictionary<string, string> result in ViewBag.Jobs)
{
    <table class="job-listing">
        @foreach (KeyValuePair<string, string> pair in result)
        {
            <tr>
                <td>@pair.Key</td>
                <td>@pair.Value</td>
            </tr>
        }
    </table>
}